{
  "compilerOptions": {
    "baseUrl": ".",        // important! tells Next.js/TS where to start resolving paths
    "paths": {
      "@/*": ["src/*"]    // maps "@/components/..." → src/components/..."
    }
  }
}
